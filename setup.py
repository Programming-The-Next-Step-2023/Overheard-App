import setuptools

setuptools.setup(
    name = "what's new?",
    version = "0.0.1",
    author = "nitya",
    author_email = "nitya.shah@student.uva.nl",
    python_requires= ">=3.6",
    packages = setuptools.find_packages()
)